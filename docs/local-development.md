# Local Development

## Environment Files

Copy `.env.example` to `.env.local` for local secrets.

Do not commit `.env.local`, `.env`, connection strings, database passwords, API keys, or tokens.

## Neon Database

The project uses Neon Postgres for the development database.

Current Neon branch model:

- `production` is the future production branch.
- `dev` is the shared development branch.
- Future feature or preview branches should be created deliberately from the appropriate parent branch.

The local `.neon` file is Neon CLI-generated local tool state. Keep it gitignored. It may contain org/project context and should not contain a branch ID in committed project context.

Neon agent skill snapshots generated by the Neon CLI should not be committed. Use current Neon docs, the Neon CLI, or the Neon MCP server on demand instead of storing third-party guidance snapshots in this repo.

## Database Setup

After creating or checking out the intended Neon branch, pull local environment variables:

```txt
npx -y neonctl env pull
```

This writes Neon variables such as `DATABASE_URL`, `DATABASE_URL_UNPOOLED`, and `NEON_BRANCH` to `.env.local`.

Apply committed migrations to the selected database branch:

```txt
set -a; source .env.local; set +a; pnpm db:migrate:deploy
```

Validate the Prisma schema and generate the Prisma client:

```txt
pnpm db:validate
pnpm db:generate
```

Run the normal project checks:

```txt
pnpm test
pnpm typecheck
```

## Migration Discipline

The Prisma schema is the source of truth for database shape.

Migration SQL files are generated artifacts. Review them, but never hand-edit them.

If generated SQL is wrong, update the Prisma schema or Prisma config and regenerate.
